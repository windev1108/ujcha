/** Biến môi trường — OAuth Client ID (Web / iOS / Android) dùng khi verify idToken */
export const GOOGLE_AUTH_ENV = {
  /** Client ID từ Google Cloud Console (phải trùng audience token từ frontend) */
  CLIENT_ID: 'GOOGLE_CLIENT_ID',
} as const;
